// router.js
const express = require('express');
const router = express.Router();

// تعریف مسیرها در اینجا
router.post('/api/register', async (req, res) => {
  // کد ثبت‌نام
});

router.post('/api/login', async (req, res) => {
  // کد ورود
});

// سایر مسیرها...

module.exports = router;
